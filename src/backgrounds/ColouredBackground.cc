#include "ColouredBackground.hh"

#include <SDL.h>

#include "../assets.hh"


//sets the colour that it fills the background with
ColouredBackground::ColouredBackground(char pRed,char pGreen,char pBlue,
                                       char pAlpha)
{

};

//might be useful for animated backgrounds
void ColouredBackground::update(float deltaTime)
{

};

//display the background with dynamic width and height
void ColouredBackground::render(SDL_Renderer * renderer,int screenWidth,
                                int screenHeight)
{

};
